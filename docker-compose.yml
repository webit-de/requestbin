app:
    build: .
    environment:
        REALM: prod
        REDIS_URL: "//redis:6379"
    links:
        - redis
    ports:
        # Change default port to localhost:8084
        - "8084:8000"
    # restart on reboot
    restart: always

redis:
    image: redis
